<div id="job-view-container" class="container">
    <div class="row text-center">
        <a class="back-link main-color" href="/">Voltar</a>
       
        <div class="col-md-12" id="job-content">
         <div id="img-container" class="col-md-12">
             <img src="/img/home.svg" alt="company" />
         </div>
         <h1 class="main-color">{{job.dataValues.title}}</h1>
            <p class="bold">Quem a empresa está buscando:</p>
            <p><span class="job-desc">{{job.dataValues.description}}</span></p>
            <p><span class="bold">O salário é de: </span>R$ {{job.dataValues.salary}}</p>
            <p>
                Para trabalhar na empresa <span class="bold main-color">{{job.dataValues.company}}</span>.
                Envie um email para <a href="mail:to{{job.dataValues.email}}">{{job.dataValues.email}}</a>
            
            </p>
        </div>

    </div>
</div> 